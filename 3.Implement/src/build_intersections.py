from Point import Point
from Line import Line

from check_aPoint import check_aPoint



class build_intersections():
    def __init__(self, lines, some_crosspoint):
        self.lines = lines # list
        self.some_crosspoint = some_crosspoint # list


def build_intersections(lines):
    display_Acrosspoint

    return Cpoints

def sort_Points(some_crospoint):


    return points


if __name__ == '__main__':
    N = 6
    p1 = Point(0, 0)
    p2 = Point(2, 5)
    p3 = Point(4, 7)
    p4 = Point(5, 5)
    p5 = Point(0, 1)
    p6 = Point(3, 5)

    M = 5
    lines = []
    lines.append(Line(p1, p4))
    lines.append(Line(p1, p6))
    lines.append(Line(p2, p5))
    lines.append(Line(p1, p5))
    lines.append(Line(p2, p6))


    # print(lines[0].p1.x)
